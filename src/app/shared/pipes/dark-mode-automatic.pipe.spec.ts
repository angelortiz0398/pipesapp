import { DarkModeAutomaticPipe } from './dark-mode-automatic.pipe';

describe('DarkModeAutomaticPipe', () => {
  it('create an instance', () => {
    const pipe = new DarkModeAutomaticPipe();
    expect(pipe).toBeTruthy();
  });
});
